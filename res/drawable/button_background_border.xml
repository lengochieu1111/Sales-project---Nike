<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFFFFF" /> <!-- Màu nền của button -->
    <stroke
        android:width="1dp"
        android:color="@color/pale_gray" /> <!-- Màu của đường viền -->
</shape>